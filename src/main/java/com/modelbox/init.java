package com.modelbox;

import javafx.application.Application;

/**
 * Provides an entry point for the program to run
 */
public class init {

    /**
     * Allows the JavaFX application to launch
     * @param args an array of Java Strings containing the command line arguments
     */
    public static void main(String[] args) {
        Application.launch(app.class, "");
    }
}
